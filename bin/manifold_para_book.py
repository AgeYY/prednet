# this book stores parameter values for different datasets

## random dot
#lt0_mesh = np.linspace(0, 8, mesh_size) # the range should be larger than data
#lt1_mesh = np.linspace(0, 6, mesh_size)
#lt_mesh = [lt0_mesh, lt1_mesh]
#kernel_width = [0.7, 1]
